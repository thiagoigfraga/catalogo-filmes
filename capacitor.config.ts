import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'catalogo-filmes',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
